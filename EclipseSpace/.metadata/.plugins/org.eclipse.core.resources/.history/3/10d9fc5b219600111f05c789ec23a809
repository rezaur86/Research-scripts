import psycopg2

db_name = 'twitter'
db_server = 'localhost'


def openDb(auto_commit=True, change_schema=True):
    con = psycopg2.connect(database=twitter, user='rezaur', password='acer1986', host=db_server, port=5432)
    con.autocommit = auto_commit
    cur = con.cursor()
    if change_schema:
        cur.execute('set search_path=%s,%s', (db_namespace, 'public'))
    return (con, cur)
